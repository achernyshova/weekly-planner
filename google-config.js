/*
 * Google OAuth configuration for Weekly Planner.
 *
 * Replace the empty string with the Web application OAuth Client ID from
 * Google Cloud Console, then commit and deploy this file.
 *
 * OAuth client IDs are public identifiers. Do not place an OAuth client secret,
 * API key, access token, or any other secret in this static application.
 */
window.WEEKLY_PLANNER_CONFIG = {
  googleClientId: ''
};
